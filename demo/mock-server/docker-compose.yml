version: "3.9"

services:
  mock-server:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gradebuddy-mock-server
    ports:
      - "3000:3000"
    restart: unless-stopped
